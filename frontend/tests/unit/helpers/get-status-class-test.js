import {
  getStatusClass
} from 'frontend/helpers/get-status-class';

module('GetStatusClassHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = getStatusClass(42);
  ok(result);
});
